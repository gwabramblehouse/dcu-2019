Home Mobility Monitoring.

Code for monitoring temperature at top of head analogue.
Tests that need it can query the specific sensor node.

./bin : script run as systemd service.
./etc : systemd configuration (for service).
./src : C source for I2C code to read DS3231.
./var : sample logs.
